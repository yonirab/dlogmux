# If a container has a label with this key, we log it
LOG_STRATEGY_KEY=io.logmux.strategy

# The fs storage method stores logs in this file
FS_STORE_PATH=/mnt/messages

# The directory on the host with the logfile generated by the fs storage method
HOST_LOGFILE_DIR=/workspace/dlogmux/logs